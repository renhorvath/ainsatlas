FIRECRAWL_API_KEY=
ANTHROPIC_API_KEY=

# Copy to .env locally. Add the same two keys in Vercel → Settings → Environment Variables.
# Optional: Vercel → Storage → Blob for durable storage across server restarts.
