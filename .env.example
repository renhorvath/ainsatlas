FIRECRAWL_API_KEY=
ANTHROPIC_API_KEY=

# Local Python pipeline (also set as GitHub Actions secrets for atlas-pipeline.yml)
# After export, commit web/data/*.json — or let the GitHub Action commit them.

# Optional — enable Add conference + Run pipeline on production Vercel:
# GITHUB_TOKEN=   # PAT with repo contents + actions (workflow dispatch)
# GITHUB_REPO=renhorvath/ainsatlas
