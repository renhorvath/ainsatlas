{
  "private": true,
  "name": "ainsatlas",
  "description": "Next.js app is in web/. Vercel: set Root Directory to web (recommended). These deps satisfy version detection if the project root is the repo.",
  "engines": {
    "node": ">=20.9.0 <23"
  },
  "dependencies": {
    "next": "^15.5.7",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web"
  }
}
